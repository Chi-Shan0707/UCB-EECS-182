## Translation

used paired sentences to train model? (english $\lefrightarrow$ french)


standard

zero-shot machine translation

English-thought  -thought french

That is  a representation



Machine Learning
We get x, we want f(x)=y;

a function is a set of rules.
Programming is trying to define rules by hand.

But what if we don't know rules?
and rules have many excepetions special cases or too complex?

We have to define  a program that acquires this relationship from data.


If rules are too complex and full of sppcial cases & exceptions,
it's easer to povide data or examples thjant o implement those rules

Questions : Does this also apply to human and animal leanrning?


shallow learning  -> we earn 

input(x) ->  $\phi(x)$ -> features -> $\phi(x)^T \theta \le 0$ -> label


all the parameters are in $\phi(x)^T \theta \le 0$

The challenge is in programming the features

But why don't we learn parameters in $\phi(x)$ and automatically